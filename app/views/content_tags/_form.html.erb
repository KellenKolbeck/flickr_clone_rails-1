<!-- <%= form_tag(edit_photo_content_tag_path, method: 'post', multipart: true) do %>

  <div class="form-group">
    <%= label_tag "Tags" %>
    <%= text_field_tag :name %>
  </div>

  <%= submit_tag "Edit Tag", class: "btn btn-success" %>

<% end %> -->

<%= form_for [@photo, @content_tag] do |f| %>

  <div class="form_group">
    <%= f.label "Tag" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <%= f.submit "Edit", class: "btn btn-default" %>
<% end %>
